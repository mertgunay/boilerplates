# postgresql.conf — overrides for this stack.
#
# Same conventions as compose/postgres/postgresql.conf — see that file for
# the full set of commented examples (memory, connections, WAL, logging,
# autovacuum). This file starts empty so the image defaults apply.
#
# Locale note: the cluster locale (LC_COLLATE, LC_CTYPE, etc.) is set at
# initdb time via POSTGRES_INITDB_ARGS in compose.yml, NOT here. Changes
# to those values here have no effect after the cluster is created.
