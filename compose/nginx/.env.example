# Standalone nginx environment template.
# Copy to .env if you want to override the defaults below. The stack
# works fine without an .env file.

# Optional: published port overrides.
# NGINX_HTTP_PORT=80
# NGINX_HTTPS_PORT=443
